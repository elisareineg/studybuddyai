"use client";
import React from 'react';
import { FiRefreshCw } from 'react-icons/fi';

export default function Quiz({ flashcards }) {
  // No quiz form, just return null or a message
  if (!flashcards || flashcards.length === 0) return null;
  return null;
} 